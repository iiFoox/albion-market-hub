# HEPHAESTUS v8.1.0 — Core Contract Alignment and Drift Guard

> Release Date: 2026-04-28
> Package: `HEPHAESTUS-Framework-v8.1.0.zip`
> Scope: Compact core contract alignment and drift validation

---

## Summary

v8.1.0 keeps the always-loaded framework contract current, compact, and release-validated. It aligns `AGENTS.md` with the current v8 operational surface and adds a guard against future drift.

## Added

- `.agents/protocols/core-contract-drift-guard-protocol.md`
- `.agents/tools/check-core-contract.ps1`
- `.agents/docs/en/25-CORE-CONTRACT-DRIFT-GUARD.md`
- `.agents/docs/pt-br/25-GUARDA-DE-DERIVA-DO-CONTRATO-CENTRAL.md`

## Changed

- Compacted `.agents/AGENTS.md` by removing broad always-loaded technology catalogs.
- Updated `.agents/AGENTS.md` to reference the real inter-agent communication bus.
- Added `core_contract_drift_guard` to active framework config.
- Added a `core-contract` Smart Loading group.
- Integrated the checker into `doctor`, manifest validation, and the pre-release gate.

## Safety and Compatibility

- No existing tool entry points were removed.
- No project execution behavior changed.
- Technology-specific guidance remains available through project adapters, profiles, and conditional knowledge.
- The new checker only reads framework files and reports drift.

## Validation

Expected release validation:

- `check-core-contract.ps1`
- `hephaestus.ps1 -Action validate`
- `check-doc-parity.ps1`
- `validate-framework.ps1 -StrictReferences`
- `hephaestus.ps1 -Action gate -Version 8.1.0`
- Package gate with `HEPHAESTUS-Framework-v8.1.0.zip`
