# Documentation Source Notes

> Framework Version: 8.7.0
> Purpose: Keep bilingual documentation coordinated without duplicating the operational framework core.

---

This folder is reserved for documentation source-of-truth notes and future generated reference inputs.

Current v8.7.0 behavior:

- paired EN/PT-BR files are tracked in `.agents/docs/_translation-map.yaml`;
- parity is validated by `.agents/tools/check-doc-parity.ps1`;
- operational framework files remain outside the docs source tree.

The framework does not generate translations automatically yet. It validates that the bilingual structure stays aligned.











